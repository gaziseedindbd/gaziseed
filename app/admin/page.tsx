import {createClient} from '@/lib/supabase/server';
export default async function AdminDashboard(){const s=await createClient();const [{count:products},{count:orders},{count:customers},{data:revenueRows}]=await Promise.all([s.from('products').select('*',{count:'exact',head:true}).eq('active',true),s.from('orders').select('*',{count:'exact',head:true}),s.from('customers').select('*',{count:'exact',head:true}),s.from('orders').select('total').eq('payment_status','paid')]);const revenue=(revenueRows||[]).reduce((n:any,o:any)=>n+Number(o.total||0),0);const cards=[['Active Products',products??0],['Orders',orders??0],['Customers',customers??0],['Paid Revenue',`৳${revenue.toLocaleString()}`]];return <main className="p-5 md:p-8"><div><p className="font-bold text-[#1f6b3b]">GAZI SEED</p><h1 className="mt-1 text-3xl font-black">Admin Dashboard</h1><p className="mt-2 text-gray-500">Manage your Bangladesh store.</p></div><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label,value])=><div key={String(label)} className="rounded-2xl border bg-white p-6"><p className="text-sm text-gray-500">{label}</p><p className="mt-2 text-3xl font-black">{value}</p></div>)}</div><div className="mt-8 rounded-2xl border bg-white p-6"><h2 className="text-xl font-black">Quick Actions</h2><div className="mt-4 flex flex-wrap gap-3"><a href="/admin/products/new" className="rounded-xl bg-[#1f6b3b] px-5 py-3 font-bold text-white">+ Add Product</a><a href="/admin/products" className="rounded-xl border px-5 py-3 font-bold">Manage Products</a></div></div></main>}
