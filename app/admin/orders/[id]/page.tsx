'use client';
import {useEffect,useState} from 'react'; import {createClient} from '@/lib/supabase/client'; import {useParams,useRouter} from 'next/navigation';
const statuses=['pending','confirmed','processing','packed','shipped','delivered','cancelled','returned']; const payments=['pending','paid','failed','refunded','cod'];
export default function AdminOrderDetail(){const {id}=useParams<{id:string}>();const r=useRouter();const [o,setO]=useState<any>();const [items,setItems]=useState<any[]>([]);const [history,setHistory]=useState<any[]>([]);const [msg,setMsg]=useState('');const [note,setNote]=useState('');const load=async()=>{const s=createClient();const [{data:order,error},{data:its},{data:h}]=await Promise.all([s.from('orders').select('*').eq('id',id).maybeSingle(),s.from('order_items').select('*').eq('order_id',id),s.from('order_status_history').select('*').eq('order_id',id).order('created_at',{ascending:false})]);if(error)setMsg(error.message);setO(order);setItems(its||[]);setHistory(h||[])};useEffect(()=>{load()},[id]);async function updateStatus(status:string){const s=createClient();const {error}=await s.rpc('admin_update_order_status',{p_order_id:id,p_new_status:status,p_note:note||null});setMsg(error?.message||'Status updated');setNote('');if(!error)load()}async function updatePayment(status:string){const s=createClient();const {error}=await s.rpc('admin_update_payment_status',{p_order_id:id,p_status:status,p_note:note||null});setMsg(error?.message||'Payment updated');if(!error)load()}if(!o)return <main className="p-8">Loading…</main>;const customer=o.guest_name||'Registered customer';return <main className="p-5 md:p-8"><button onClick={()=>r.push('/admin/orders')} className="font-bold text-[#1f6b3b]">← Orders</button><div className="mt-3 flex flex-wrap justify-between gap-4"><div><h1 className="text-3xl font-black">{o.order_number}</h1><p className="mt-1 text-gray-500">{new Date(o.created_at).toLocaleString()} · {o.country} · {o.source}</p></div><div className="rounded-xl bg-white px-4 py-3 font-bold">{o.currency_code} {Number(o.total).toLocaleString()}</div></div><div className="mt-8 grid gap-6 lg:grid-cols-3"><section className="rounded-2xl border bg-white p-6 lg:col-span-2"><h2 className="text-xl font-black">Order Items</h2><div className="mt-4 divide-y">{items.map(i=><div key={i.id} className="flex justify-between gap-4 py-4"><div><p className="font-bold">{i.product_name}</p><p className="text-sm text-gray-500">SKU: {i.sku||'—'} · Qty: {i.quantity}</p></div><b>{o.currency_code} {Number(i.total_price).toLocaleString()}</b></div>)}</div><div className="mt-5 grid gap-2 border-t pt-5 text-sm"><div className="flex justify-between">Subtotal <b>{o.subtotal}</b></div><div className="flex justify-between">Discount <b>-{o.discount}</b></div><div className="flex justify-between">Shipping <b>{o.shipping_fee}</b></div><div className="flex justify-between text-lg font-black">Total <b>{o.total}</b></div></div></section><aside className="space-y-6"><section className="rounded-2xl border bg-white p-6"><h2 className="font-black">Customer & Shipping</h2><p className="mt-3 font-bold">{customer}</p><p>{o.guest_phone||''}</p><p>{o.guest_email||''}</p><pre className="mt-3 whitespace-pre-wrap text-sm text-gray-600">{JSON.stringify(o.shipping_address,null,2)}</pre></section><section className="rounded-2xl border bg-white p-6"><h2 className="font-black">Update Order</h2><textarea value={note} onChange={e=>setNote(e.target.value)} placeholder="Admin note" className="mt-3 w-full rounded-xl border p-3"/><select value={o.status} onChange={e=>updateStatus(e.target.value)} className="mt-3 w-full rounded-xl border p-3">{statuses.map(x=><option key={x}>{x}</option>)}</select><select value={o.payment_status} onChange={e=>updatePayment(e.target.value)} className="mt-3 w-full rounded-xl border p-3">{payments.map(x=><option key={x}>{x}</option>)}</select>{msg&&<p className="mt-3 text-sm text-[#1f6b3b]">{msg}</p>}</section></aside></div><section className="mt-6 rounded-2xl border bg-white p-6"><h2 className="text-xl font-black">Order Timeline</h2><div className="mt-4 space-y-4">{history.map(h=><div key={h.id} className="border-l-2 pl-4"><p className="font-bold">{h.old_status||'new'} → {h.new_status}</p><p className="text-sm text-gray-500">{new Date(h.created_at).toLocaleString()}{h.note?` · ${h.note}`:''}</p></div>)}</div></section></main>}
