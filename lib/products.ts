import { createClient } from '@/lib/supabase/server';
export async function getActiveProducts(country:'BD'|'IN'){const s=await createClient();return s.from('products').select('id,name_en,name_bn,slug,sku,regular_price,sale_price,short_description,category_id,featured,bestseller,is_new,seasonal,product_images(id,optimized_url,source_url,sort_order),product_variants(id,name,price,sale_price,stock,sku,active)').eq('active',true).eq('country',country).order('featured',{ascending:false}).order('created_at',{ascending:false});}
